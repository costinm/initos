#!/bin/sh

# Following alpine docs for busybox init autologin
# ( init doesn't seem to allow the command line args )
# login will run the
# /etc/login.defs has settings.
# Use with (in inittab or script):
# /sbin/getty -n tty1 /sbin/autologin.build
export HOME=/home/build
exec login -f build -p
